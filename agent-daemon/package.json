{
  "name": "agent-daemon",
  "version": "0.1.0",
  "description": "AI Agent Commander - Agent Daemon (runs on managed servers, drives local Codex CLI under Hub control)",
  "private": true,
  "license": "UNLICENSED",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev:server": "node dev-scripts/mock-hub.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "socket.io-client": "^4.8.0"
  },
  "devDependencies": {
    "socket.io": "^4.8.0"
  }
}